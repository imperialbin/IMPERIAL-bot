import { IUser, Users } from "../models/User";
import { Message } from "discord.js";

// Utilities
import { throwSuccess } from "../utilities/throwSuccess";
import { throwError } from "../utilities/throwError";

export const setApiToken = async (msg: Message) => {
};
